Session 24 — the focus & keyboard-contract pass, continuing from session 22's verified default-palette parity and the user's r14 transcript (main @ 6fbb81c, 260 tests green, r12–r14 contracts pinned; 6fbb81c added the user's r14 transcript as docs/session_23.md, whose suggested next step — "extend the sweep to keyboard/focus-state contracts" — became this round's probe dimension).

Started from the pushed r14 tree (fresh clone after a workspace reset): core docs re-read and validated against the codebase (README / AGENTS / CLAUDE / PAD v1.13 / session_22 / session_23), baseline gates green (lint ✓, typecheck ✓, 260/260 ✓), `.env.example` re-verified against the codebase's sole environment variable (`DATABASE_URL`), and the 8 `docs/screenshots/` re-verified present. Both browser sessions (live + clone) were re-established at 1536×844 and 390×844 — the live account pristine (PROJECTS 0 / SUPPLIES 0 / INSPO 15).

Reconnaissance (live vs clone, 2026-09-19, fresh this session):

- 8 paired captures (desktop dashboard / projects / supplies / inspiration, mobile dashboard / sidebar drawer / chat drawer, login — the pointer parked at a verified non-hoverable spot for every capture, per the r13 hygiene rule) with VLM comparisons: **8/8 PARITY**; the two pairs carrying flags map 1:1 onto the documented accepted set (supplies-desktop: header email + dev badge; chat-drawer: the live's residue message + its input row pushed below the fold by the documented 78px scrollHeight delta, re-measured this session at 928 vs 850).
- Pixel structural diffs: 0.24–1.15% hot per pair — the SAME values r13/r14 measured per pair (dashboard-desktop 0.72%, chat-drawer 1.15%, login 0.24%), every cluster classified into the documented accepted set; the mobile bottom-left cluster was crop-inspected and confirmed to be the dev-tools "N" badge (the documented dev-mode artifact).
- Functional probes: the pristine-state export payload byte-identical (121 bytes both sides, modulo the timestamp); 23/23 smoke checks on the dev server.
- Regression checks: the r12 lavender active-tile fix intact (oklab(#b78bff)/0.6|0.1 ≡ the live's rgba(183,139,255,.6|.1), geometry 106×78 @ (257,173)); the r13 memory-button fix intact (idle border #5b3fd3/30, hover trio engaging with text rgb(244,242,122) byte-exact); the r14 default-palette pins intact (the Sign Out border/text and all three email-gradient stops byte-identical rgb forms).
- NEW probe dimension (this round's extension): a systematic **keyboard/focus-state contract sweep** — DOM tab-order enumeration (24 tabbables in the IDENTICAL sequence on both sides), computed focus styles on every sweep station, rendered ring pixels, tabindex-attribute comparison, and Escape/focus-management probes on the modals and drawers. Inputs' focus borders verified byte-exact (the chat input's rgb(46,196,182)); both sides' mobile drawers ignore Escape (parity); the color-scheme matches (`normal` both, so the UA ring renders identically).

**r15-F1 (Medium) — the clone authored a universal focus-outline color the live never renders.** The shadcn scaffold's base rule — `@layer base { * { @apply border-border outline-ring/50 } }` — set `outline-color` to the turquoise `--color-ring` token (#2ec4b6) at 50% on EVERY element. The live's compiled CSS carries no universal outline-color rule at all (its only outline rules are TW3's transparent `focus:outline-none` utility and Amplify's login chrome), so every keyboard-focused button/tile/link on the live renders the BROWSER DEFAULT ring — computed `rgb(16,16,16)` in this Chromium, a near-black band. The clone rendered a bright turquoise ring instead: canvas-verified, the Sign Out focus-ring crop diffed 496 hot px, the ring band sampling #2ec4b6 at 50% over the #050009 canvas (150,225,218 → 25,98,95) vs the live's near-black band. The divergence is invisible in pointer-driven captures (focus rings render only on keyboard focus) — the same class as r13-F1's hovers, which is exactly why ten prior rounds of screenshot diffs never surfaced it and why the DOM-level sweep was built.

**r15-F2 (Medium) — the clone's create modals carried keyboard affordances the live lacks.** Measured on the live: opening "New Project" leaves focus on the trigger button (no focus management — the modal is a plain div overlay with no role), and pressing Escape does nothing (the overlay stays; only the ✕ button and the scrim click dismiss it). The clone's modals had added a `window` Escape keydown handler AND an initial focus steal into the first input — behavioral divergences a keyboard user directly experiences (the r13-F2 precedent: interactive behavior follows the live). The clone's `role="dialog"` + `aria-modal` are invisible semantics — the drawer-landmark precedent — and stay.

Remediation (TDD — RED first, then GREEN, then verified at four levels):

- `src/lib/focus-fidelity.test.ts` (new, 11 pins): the base-layer `outline-ring/50` negative (comments stripped first so the docblock that DOCUMENTS the removed rule cannot trip the pin), the `border-border` positive (the inert border-color default stays — no element uses a bare `border` class, r14-verified), the docblock marker, and 8 modal-contract pins (no `"Escape"`, no `window.addEventListener`, no `)?.focus()` in either modal; `role="dialog"` + `aria-modal="true"` kept in both). RED: 8 failed / 3 passed; GREEN: **271/271** (260 + 11).
- `src/app/globals.css`: `outline-ring/50` removed from the base rule (docblock records the r15 focus-ring parity contract and the do-not-re-add warning); `border-border` stays.
- `src/components/studio/project-modal.tsx` + `supply-modal.tsx`: the Escape keydown handler, the initial-focus call, and the now-dead `dialogRef` removed (with an in-place comment documenting the live's measured no-affordance contract); `role="dialog"` + `aria-modal` kept; the ✕ and scrim-click dismissal paths untouched.

Verification: lint ✓, typecheck ✓, **271/271 tests** ✓, clean production build ✓. Browser-verified end-to-end on the dev server: the clone's focused Sign Out now computes `outline-color: rgb(16,16,16)` — byte-identical to the live's UA default — and the rendered ring crop diffs **0 hot px** (band pixels rgb(16,16,16) on both sides at every sampled row; was 496). Both modals: focus stays on the trigger (`focusInDialog: false`), Escape is a no-op (the dialog persists), the Close button still dismisses, and the dialog semantics remain. A post-fix paired dashboard capture re-diffed at the unchanged **0.72%** (an initial 2.55% reading decomposed into chat-column scroll session state — a clean reload of both pages restored the baseline; the fixes themselves are invisible in pointer-driven captures, as expected). The 23/23 smoke suite re-run green post-fix. **Production-server probe** (standalone build on :3100): the focused element computes rgb(16,16,16), the modal contract behaves identically, `document.fonts.size === 0`, and no dev-tools portal exists — the production ground truth is clean. Note: the emitted CSS still contains an INERT `.outline-ring\/50` utility rule (Tailwind v4's scanner picks the class string out of the test file's own text) — no DOM element carries the class, so nothing renders it; accepted as the r14 equivalence class of inert emissions.

Deliverables beyond the fixes: the 8 `docs/screenshots/` re-shot on the remediated dev server (login, desktop dashboard/projects/supplies/inspiration, mobile dashboard/sidebar drawer/chat drawer — pointer parked neutral for every capture); `.env.example` verified to match the codebase (`DATABASE_URL` only, unchanged).

Documentation aligned: README (r15 status row, 271-test counts, the design table's focus-outline row, the test-suite description), AGENTS.md (271 tests + the keyboard-contract framework quirk), CLAUDE.md (271 tests, the focus-fidelity test-suite bullet, and the a11y bullet rewritten — the modal keyboard affordances and the authored focus ring are now documented as REMOVED parity contracts, with role="dialog"/aria-modal and the landmarks kept as the invisible-semantics class), PAD v1.14 (revision block [R15], §5.2 focus-outline row, §7.1 unit distribution 243 + 28, §10 two resolved rows), and this session record.

**Session 24 complete — the clone's keyboard contract now matches the live's measured no-affordance behavior exactly: the browser-default focus ring renders on every element (byte-identical pixels), the create modals neither close on Escape nor steal focus, the tab order is verified identical, and the input focus chrome remains byte-exact. Every steady-state surface re-confirmed at parity on fresh paired evidence; the r11 supply contracts, the r12 stat-tile accents, the r13 hover/hyphen-family fixes, the r14 default-palette pins, and the r10 drawer chrome remain pinned.**

**Verification:** all claims executed and observed this session; the live account is pristine (read-only recon — no mutations beyond the sign-in/sign-out flow); the accepted divergences remain documented (focus-ring shorthand history aside — now resolved, dev badge, account data, the live's residue message with its 78px chat-column offset, the mobile login card's 0.5px fractional offset, the nav-vs-aside drawer landmark, dev-mode-only browser artifacts, sub-perceptual oklab-vs-rgba compositing and fractional text-row rounding).

**Suggested next steps:** watch the CI verify-gate run on the pushed commit; the keyboard/focus sweep (`scripts/r15_focus_sweep.sh` + the pixel probe pattern, evidence under `download/parity-evidence/r15/`) is now the template for future recons alongside the r14 hover sweep — computed-style-level comparison catches what captures cannot; candidate dimensions still unexplored: screen-reader announcement order (live-region behavior), reduced-motion rendering, and print stylesheet parity; re-shoot `docs/screenshots/` after any chrome-level change (pointer parked neutrally).
