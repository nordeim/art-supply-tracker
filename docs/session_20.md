Session 20 — the hover & hyphen-family pass, continuing from session 19's verified stat-tile parity (main @ 201f7f7, 238 tests green, r12 contracts pinned; 201f7f7 added the user's r12 transcript as docs/session_19.md).

Started from the pushed r12 tree: core docs re-read and validated against the codebase (README / AGENTS / CLAUDE / PAD v1.11 / session_18 / session_19), baseline gates green (lint ✓, typecheck ✓, 238/238 ✓), `.env.example` verified against the codebase's sole environment variable (`DATABASE_URL`). The :3000 server was switched from the leftover r12 production probe back to the dev server, and both browser sessions (live + clone) were re-established — the live account pristine (PROJECTS 0 / SUPPLIES 0 / INSPO 15).

Reconnaissance (live vs clone, 2026-09-19, fresh this session):

- 8 paired captures (desktop dashboard / projects / supplies / inspiration, mobile dashboard / sidebar drawer / chat drawer, login) with VLM comparisons: ALL PARITY. Two initial GAP flags were traced to capture-state errors of the operator's own making and re-captured cleanly: a stale-ref click had left the live on the Inspiration view (the mobile dashboard pair compared different views), and the re-login had left the live page scrolled 138px (the header scrolled out of view behind the sticky toggle bar — resetting scroll to 0 and re-capping both drawers on matched dashboard backgrounds resolved every flag).
- Pixel structural diffs: 0.23–1.14% hot pixels per pair, every cluster classified into the documented accepted set — the live's residue chat message (the chat drawers' scrollHeights differ by exactly the documented 78px: 928 vs 850, which also pushes the live's chat input row below the drawer fold), the dev-mode-only Next.js badge, the header email area, the Partners card's 0.69–1px fractional text rounding, and login-page text antialiasing (VLM-verified: identical text, sub-pixel rendering only).
- Functional probes: the pristine-state export payload re-confirmed byte-identical (121 bytes both sides, modulo the timestamp); the sign-out flow exercised on both sides; 23/23 smoke checks on the dev server; the r12 lavender active-tile fix re-verified intact (classes, geometry 106×78 @ (257,173), computed colors).
- The Studio Memory (✧) popover pair: popover region 0.000% hot pixels — but the probe (with the pointer resting on the live's button) surfaced the session's two real findings.

**r13-F1 (High) — the header memory button carries the live's HYPHENATED utility families.** The live app ships TWO parallel Tailwind color systems in its compiled CSS: the UNDERSCORED utilities (`ast_purple` #5a3a8e, `ast_yellow` #ffd5a8 — the "utility values" every other surface uses and our @theme tokens pin) and HYPHENATED families (`ast-purple`, `ast-yellow`) whose complete emitted rule set is exactly four selectors, all on the "✧ What was I working on?" button, and which resolve the live's `:root` variable values (previously believed vestigial): `.border-ast-purple\/30` → `#5b3fd34d`, `.hover\:border-ast-yellow\/70:hover` → `#f4f27ab3`, `.hover\:bg-ast-yellow\/20:hover` → `#f4f27a33`, `.hover\:text-ast-yellow:hover` → `rgb(244 242 122)`. The clone had copied the button's class string verbatim, so its hyphenated classes resolved to the UTILITY values — the wrong idle border color (always visible: #5a3a8e vs the live's #5b3fd3) and all three hover colors (visible on any real pointer device). Scope-bounded by a full-bundle scan of the live's CSS (the four rules are the only hyphenated ast-* utilities) plus a live rail-button probe confirming the other surfaces use the underscored families (= the clone's values).

**r13-F2 (Medium, framework-level) — Tailwind v4's hover variant is media-guarded.** The clone's `hover:` utilities compiled inside `@media (hover: hover)` while the live's Tailwind v3 emits plain `:hover` selectors. Consequences: on touch devices the live's hover tints apply and stick on tap while the clone's never engaged; in headless captures the clone's hovers never rendered, making hover parity unverifiable by screenshot. (This is also why every prior round's hover class-string comparisons never caught r13-F1 — the colors never rendered on the clone side in any capture.)

Remediation (TDD — RED first, then GREEN, then browser-verified against the live):

- `studio-app.tsx`: the memory button now carries literal arbitrary values — `border-[#5b3fd3]/30` idle, `hover:border-[#f4f27a]/70 hover:bg-[#f4f27a]/20 hover:text-[#f4f27a]` — the second sanctioned literal-color exception alongside the login card's `border-[#5B3FD3]`, with a docblock explaining the hyphen-family provenance.
- `globals.css`: `@custom-variant hover (&:hover);` restores the live's plain-`:hover` semantics (validated empirically — the override syntax had to be proven against Tailwind 4.1.18 before planning around it; the initial no-op turned out to be a wedged Turbopack dev server serving stale CSS, fixed by a clean `.next` restart). The theme comment's disproven "no utility or custom rule ever resolves those [ :root values ] — they are dead values" claim was corrected in place.
- New `src/lib/header-button-fidelity.test.ts` (9 pins): the four literal classes, the verbatim non-color class run, negative pins on the utility families (the r13-F1 bug), the variant override, and the corrected comment. RED: 8 failed / 1 passed (the variant pin — its fix had landed as the necessary empirical spike); GREEN: 9/9.

Verification: lint ✓, typecheck ✓, **247/247 tests** ✓ (238 + 9 new), clean production build ✓. The emitted dev CSS carries the four literal rules with fallback declarations byte-identical to the live's (`#5b3fd34d`, `#f4f27ab3`, `#f4f27a33`, `#f4f27a`) and ZERO `@media (hover: hover)` guards; the `lab()` progressive re-declarations were canvas-verified to render pixels identical to the live's rgba values. Browser-verified end-to-end on the dev server AND a production-server probe (port 3001): hovering the clone's button now computes `hover:bg` = the live's `#f4f27a/20`, `hover:text` = `rgb(244, 242, 122)` byte-exact, production serves zero webfonts and no dev badge. Fresh paired popover capture (pointer deliberately resting on the button on BOTH sides — both hovered, popover open at byte-identical [1313,198,177,20]): VLM PARITY, popover region 0.00% hot, button interior samples identical ([53,48,30] both sides); the residual button-region diff is the glyph shift from the documented email-width delta (best alignment ±2px, color sequences identical). Post-fix dashboard pair re-diffed at the unchanged 0.72% — no regression from un-guarding hover. Evidence in `download/parity-evidence/r13/`. The live account was left pristine (read-only recon).

Deliverables beyond the fixes: a fresh set of 8 dev-server screenshots (login, desktop dashboard/projects/supplies/inspiration, mobile dashboard/sidebar drawer/chat drawer — pointer parked neutral for every capture) saved under the repo's `docs/screenshots/` as the visual reference; `.env.example` verified to match the codebase (`DATABASE_URL` only).

Documentation aligned: README (r13 status row, 247-test counts, design-table purple/yellow rows now carrying the hyphen-family exceptions, the screenshots/ folder in the file hierarchy), AGENTS.md (247 tests + the header-button catalog entry + two new framework quirks: the un-guarded hover variant with its capture-methodology consequence, and the hyphen-family exception), CLAUDE.md (247 tests + the fidelity bullet + the Tailwind hover-variant note + the two sanctioned literal exceptions), PAD v1.12 (revision block [R13], §5.2 color tokens with the corrected vestigial-values passage, §7.1 unit distribution 219 + the header-button-fidelity entry, §10 three new resolved rows), and this session record.

**Session 20 complete — the header memory button now renders the live's hyphen-family colors exactly (idle border #5b3fd3, hover trio #f4f27a) with emitted CSS byte-identical to the live's, and the clone's hover semantics now match the live's Tailwind v3 plain-:hover behavior on every device class. Every steady-state surface re-confirmed at parity on fresh paired evidence; the r11 supply contracts, the r12 stat-tile accents, and the r10 drawer chrome remain pinned.**

**Verification:** all claims executed and observed this session; the live account is pristine; the accepted divergences remain documented (focus ring, dev badge, account data, the live's residue message with its 78px chat-column offset, the mobile login card's 0.5px fractional offset, the nav-vs-aside drawer landmark, dev-mode-only browser artifacts, sub-perceptual oklab-vs-rgba compositing and fractional text-row rounding).

**Suggested next steps:** watch the CI verify-gate run on the pushed commit; the `docs/screenshots/` set now serves as the repo's visual reference (re-shoot after any chrome-level change — and remember the new capture hygiene: park the pointer neutrally, since clone hovers now render); if the live ever restyles its header button or re-guards its hovers, re-run the hyphen-family probes before touching the literals — the four rules are pinned per-selector, not generically.
