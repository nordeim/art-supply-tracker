Session 11 — the pixel-parity pass, continuing from session 10's verified state (main @ 2e5a41f, 134 tests green, seed fidelity pinned).

Started from the pushed tree after a workspace refresh: environment rebuilt (`db:push`, `db:seed`), all gates re-verified green (lint, typecheck, 134 tests, clean production build; skills/ excluded from every gate), and the docs (AGENTS, CLAUDE, README, PAD v1.6, session 7–10 records) re-read against the source. The 23-check smoke suite passed on the fresh tree and the live account was pristine (PROJECTS 0 / SUPPLIES 0 / INSPO 15).

Reconnaissance (live vs clone, both DOMs measured side by side):

- **The key lesson (re-learned):** the live (Vite/Amplify SPA) renders pre-hydration markup that DIFFERS from its hydrated steady state — its logo hydrates from a 320×81 inline-styled `img` to responsive classes (222×56 desktop / 159×40 mobile). All live DOM measurements were therefore taken post-hydration (≥6s wait or post-interaction); this is also why earlier sessions missed the logo and radius drift.
- Four desktop views + mobile views + the login page (desktop + mobile) captured on both apps; VLM comparison: projects/supplies/inspiration PARITY, dashboard and login with gaps root-caused below (all fixed this session).
- The accepted micro-deltas were closed: the font-build width differences (✧ button, email pill, Sign Out) all traced to ONE root cause — the clone loaded a self-hosted webfont the live does not ship.

Findings and remediation (all TDD — RED first, then GREEN, then browser-verified against the live):

1. **r8-F1 header logo (P1):** the clone's `next/image` inline `style={{ height: "auto", width: "auto", maxWidth: "320px" }}` defeated the responsive `h-10 md:h-12 lg:h-14` classes — the logo rendered 320×81 at every viewport (header 113px vs the live's 88px, every main panel shifted 25px, mobile pills pushed off-screen). Fixed: the file's INTRINSIC dimensions (1068×269) passed to `next/image` so `w-auto` keeps the true aspect, plus the live's hydrated classes `h-10 md:h-12 lg:h-14 w-auto max-w-[320px] object-contain` and no inline style. Verified: header 1920×88 / 390×126, logo (24,16) 222×56 desktop and (24,43) 159×40 mobile — byte-identical to the live.
2. **r8-F2 radius scale (P1):** the scaffold's `@theme` carried shadcn's larger radii (sm/md/lg/xl = 12/14/16/20px) while the live compiles Tailwind-v3 defaults (`.rounded-lg{.5rem}`, `.rounded-xl{.75rem}`). Fixed: the token scale corrected to 2/6/8/12px; the card radius histogram now matches the live element-for-element (24px chat panel, 12px cards).
3. **r8-F3..F6 login (P2, from the first half of the session):** the login card geometry (mx-auto in the 1fr column, 480×429, box-shadow, zero card padding with the form carrying it), equal-width 239×50 tabs, the 16px/700 submit, the 51px teal forgot row, the signup shape (Email + Password + Confirm Password — no display-name field; `deriveDisplayName` derives it from the email local-part), the Reset Password view (the live's Amplify flow with the dark-on-dark heading quirk), and the sign-in error copy ("Incorrect username or password." #660000 16px).
4. **r8-F7 mobile chat drawer scrollbar (P3):** the clone's drawer styled a `scrollbar-right` rail the live's drawer lacks — removed; both drawers now show only the browser's default overlay.
5. **r8-F8 font resolution (found during verification):** the live ships ZERO webfonts (`document.fonts` empty) — its `InterVariable, "Inter var", Inter, -apple-system, …` stack is Amplify UI's default that resolves to system fonts on every machine. The clone's `next/font` self-hosted Inter has slightly wider advance widths, which re-wrapped the Studio Memory text onto a second line and inflated the header pills. Fixed: the live's exact stack verbatim in `@theme inline`, the `next/font` import removed — every remaining micro-delta disappeared (memory text one line at (1633,222) 241×16 on both).
6. **r8-F9 chat panel structure (found during verification):** the live splits the panel into a `sticky top-4 z-10 mb-4 space-y-3` community header and a separately scrolling chat container (`scrollbar-right max-h-[calc(100vh-16rem)] space-y-4 overflow-y-auto pr-1` at desktop, plain `space-y-4` in the mobile drawer). The sticky `top-4` displacement is load-bearing — it is what positions the header 16px into view; the clone's single `space-y-3` wrapper rendered the whole column 16px high. Also: the live's chat has NO auto-scroll (no scrollTop logic in its bundle) — the clone's stickToBottom behavior was an extra and was removed; send errors render in `text-pink-300`; the message list carries no custom scrollbar styling. Fixed in `studio-app.tsx` (CommunityContent split + per-viewport scroll container) and `studio-chat.tsx` (live-bundle-verbatim classes). Verified: every community row now sits at the live's exact coordinates (121/145/198/222/276/300) and the mobile drawer matches (53/293).
7. **r8-F10..F13 login chrome completion (found via VLM + pixel sampling):** the eye toggle is the input group's right segment — 1px #89949f borders on top/right/bottom (transparent left), `0px 4px 4px 0px` radii, and the near-invisible #0d1a26 icon (the live's quirk); the password input rounds LEFT only (`4px 0px 0px 4px`); the tab strip carries a 2px top border (gray #dcdee0 with the active tab's turquoise #2ec4b6 overlay — pixel-verified as a 3px stack with the card's purple border, not 4px stacked); the input grays are exactly #89949f (not #89949b), placeholders #9ca3af (Tailwind gray-400, not a 70% blend), TYPED text renders #0d1a26 — the live's invisible-typing quirk (pixel-verified by typing into the live's field; rows 612–616 barely detectable against the card bg); and the mobile h1 renders 36px/45px (leading 1.25 → 2×45=90px; desktop stays 48/48 leading-none). All fixed and pixel-verified against the live screenshots.
8. The clone's input focus ring is deliberately KEPT (the live has no visible focus indicator at all — the clone's documented a11y hardening; transient state, invisible in comparisons).

Verification: lint ✓, typecheck ✓, 180/180 tests ✓ (134 + login-fidelity 22→26 + chat-fidelity 9 + deriveDisplayName/input pins + the radius/font token pins), 23/23 smoke checks ✓ (run twice — before and after the login chrome completion), production build from a clean `.next` ✓, and VLM parity on all four desktop views plus the login page at BOTH viewports (desktop and mobile PARITY; the mobile chat drawer verified by DOM structure since the VLM's comparison-mode claims were DOM-disproven hallucinations). Evidence archived under `download/parity-evidence/r8-*`.

Ops notes: one mid-session Turbopack crash (hot reload of a broken intermediate state) — resolved by the documented clean-restart sequence; the sandbox now reaps call-scoped background processes, so the dev server is launched double-forked (subshell + setsid → PPID 1) to survive across commands; the agent-browser daemon resets the viewport on restart (re-set 1920×1080 before pixel comparisons). The live's login page keeps its stale URL on sign-out (an SPA quirk — verify page state by DOM content, not pathname). The live's typed password text is genuinely near-invisible (#0d1a26 on #120724) — replicated faithfully.

Documentation aligned: README (r8 status row, 180-test description, login/header/chat fidelity bullets), AGENTS.md (180 tests, the zero-webfont/logo/chat-split invariants, radius note), CLAUDE.md (design-token + login/chat fidelity test bullets), PAD v1.7 (revision block [R8], §5.1 typographic system rewritten for the zero-webfont stack, §7.1 distribution 157 unit + 23 action, §10 five r8 resolution rows), and this session record.

**Session 11 complete — the clone now matches the live at pixel level: header geometry, radius scale, font resolution, chat panel structure, and the login page's full Amplify chrome are all measured-identical or pixel-verified.**

**Verification:** all claims executed and observed this session; the live account is pristine; the only accepted divergences remain documented (focus ring, dev badge, account data).

**Suggested next steps:** watch the CI verify-gate run on the pushed commit; if the live app ever bundles a webfont or changes its Amplify theme, re-run the font/stack probes before touching the clone; the r8 evidence directory holds the reference screenshots for future regression comparisons.
