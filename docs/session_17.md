Session 17 — the supply-contract pass, continuing from session 15/16's verified drawer-scrim parity (main @ 5c858de, 209 tests green, r10 scrim contract pinned).

Started from a fresh workspace: cloned the repo (HEAD 5c858de = r10's a784132 + the session_16 log), read the core docs (AGENTS, CLAUDE, README, PAD v1.9, session 15/16) and validated them against the source tree. Environment rebuilt (`bun install`, `db:push`, `db:seed`); all gates re-verified green on the fresh tree (lint ✓, typecheck ✓, 209/209 tests ✓; skills/ excluded from every gate).

Reconnaissance (live vs clone, 2026-09-19):

- Logged into the live (sepnetflix2023@outlook.com) — account pristine (PROJECTS 0 / SUPPLIES 0 / INSPO 15).
- 8 VLM comparisons (desktop: dashboard / projects / supplies / inspiration; the production build's dashboard; mobile: dashboard + both drawer pairs; login card spot-checks): every surface PARITY modulo the documented accepted deltas (account email, the live's residue chat message, the dev-mode-only Next.js badge). An initial supplies/inspiration "GAP" verdict was traced to a viewport mismatch (the live browser was still at 390×844 from the mobile capture) — re-captured with matched viewports: PARITY.
- DOM spot-checks byte-identical: header 1920×88 + logo 222×56; zero webfonts on the live; chat rows (kimsart 398/414 through testing 818/836 — identical coordinates on both); the login card 480×429 @ (494,464) with the #5B3FD3 border; the error box (49,778) 292×72 #FCE9E9; both drawer scrims `fixed inset-0 z-40 bg-black/60 md:hidden` with computed z-40 / 60% dim / no blur; both sidebar drawers 312×844 with matching class contracts; chat drawer chrome (input 183×34, Send 54×34 — the 78px y-offset is the documented residue message).
- Production build probe: `document.fonts` empty, no dev-tools badge, VLM PARITY vs the live dashboard — the dev-mode Geist faces and "N" indicator never ship.
- Pristine-state export captures byte-identical (121 bytes, same shape/field order).

The deep dive (functional probes against the live; every probe supply deleted through the real UI, the live account left pristine — verified PROJECTS 0 / SUPPLIES 0 at the end):

1. **QUANTITY contract (r11-F1, High):** the live's Add Supply modal ACCEPTS an empty quantity — the supply is created (no error), the chip renders the bare "qty" label with the "?" pill, the detail panel's Quantity row renders BLANK, and the export carries `qty: ""` / `quantity: null` / `quantityValue: null`. It validates the FORMAT: "abc" is blocked with the exact copy "Enter a valid quantity, like 2, 1.5, or 1/2". A fraction quantity ("1/2") exports `qty: 0.5` / `quantity: null` / `quantityValue: 0.5` — the `quantity` slot is the PLAIN Number() parse (no fraction support). The live's own import round-trips `qty: ""` verbatim (verified by importing a crafted payload with empty-qty, explicit-ok, and fraction supplies, then re-exporting: byte-identical). The clone diverged on every axis: blocked empty with "Quantity is required.", accepted "abc", hid the qty span when empty, rendered "—" in the detail panel, exported `qty: null` for empty and `quantity: 0.5` for fractions, and coerced imported `qty: ""` to "1".
2. **CONDITION contract (r11-F2, High):** the live's create-modal Stock Status select is INERT — a supply created with "Low" selected (via the real UI interaction API) still exports with status omitted; only the EDIT panel stores conditions. An ABSENT status renders the "?" pill (`bg-white/10 text-ast_faint`) + the unlabeled "⚠️" detail glyph and is omitted in export; an EXPLICIT "ok" (saved through the edit panel) renders the "OK" pill (`bg-ast_cyan/15 text-ast_cyan`) + the "✓ ok" icon (`bg-ast_turquoise/20 text-ast_turquoise`) and exports `status: "ok"`. The r2-era extraction assumed "live data never carries 'ok'" — disproven: edit-panel saves store it. The clone collapsed both states into "ok" and rendered "?" for both.
3. **deleteProject atomicity (r11-F3, Low):** the supply-detach and the project delete ran as two separate writes (the import path got the transaction treatment in r5; delete didn't).
4. **README Typography drift (r11-F4, Medium):** README's Design System section still documented the pre-r8 `next/font` Inter setup, contradicting AGENTS/CLAUDE/PAD's zero-webfont contract — a documentation trap for future agents.

Remediation (all TDD — RED first, then GREEN, then browser-verified against the live):

- `Supply.condition` became NULLABLE (`String?`): null = the live's absent status, "ok"/"low"/"critical" = explicit values. The create modal submits `condition: null` (the select stays rendered as visual chrome — the live's inert behavior); the edit panel initializes `supply.condition ?? "ok"` and saves the explicit value. `supplyConditionPill` gained the "ok" → cyan "OK" branch and `supplyDetailConditionIcon` the "ok" → "✓ ok" branch (both measured classes); null falls to the existing "?" / unlabeled-glyph defaults.
- The quantity contract: `isValidQuantityInput` (empty | finite number | a/b fraction) in studio-domain, enforced client-side in the modal + edit panel with the live's exact error copy and server-side by the Zod boundary (`quantity` allows "", rejects unparseable text). The chip renders the qty label unconditionally; the detail panel renders the raw (blank-when-empty) quantity.
- The wire format: `qty` = "" when empty else the fraction-aware parse; `quantity` = the plain Number() parse (null for fractions); `quantityValue` unchanged; `status` omitted when null and emitted when explicit (incl. "ok"). The import normalizer maps absent/"" → null, known tokens through, unknown tokens to the gate (rejected); `qty: ""` round-trips verbatim. Two pins that encoded the disproven assumptions were corrected with the measured values ("omits status when ok" → "omits when absent, emits when explicit"; the Jz/ternary ok-case pins → the two-state split).
- `deleteProject` now wraps the detach + delete in ONE `db.$transaction` (a mid-delete failure rolls back — pinned by a failure-injection test in the established mock pattern).
- README's Typography paragraph corrected to the zero-webfont contract; the r11 status row and the 229-test descriptions added to README/AGENTS/CLAUDE; PAD v1.10 (revision block [R11], §4.1 schema, §7.1 distribution 201 unit + 28 action, §10 four resolution rows + two new accepted rows: the nav-vs-aside drawer landmark and the dev-mode-only artifacts).

Verification: lint ✓, typecheck ✓, 229/229 tests ✓ (209 + 20 new/adjusted, incl. the new `supply-fidelity.test.ts` with 6 source pins), 23/23 smoke checks ✓, production build from a clean `.next` ✓. Browser-verified against the live contracts end-to-end: empty-qty creation renders the byte-identical chip (`NEW | … | Paint | ? | qty`) and detail panel (blank Quantity, "—" Location); the edit save flips the chip to the cyan "OK" pill and the detail to "✓ ok"; the export emits `qty:""` / `status:"ok"` exactly as the live does; the "abc" quantity is rejected with the live's exact copy. Evidence in `download/parity-evidence/r11/`. The live account was left pristine (every probe supply deleted through the real UI with native confirms; final state PROJECTS 0 / SUPPLIES 0).

Documentation aligned: README (Typography fix, r11 status row, verification row, 229-test descriptions), AGENTS.md (229 tests, the Zod-boundary two-state/quantity invariant, the three-slot wire-format invariant, the delete atomicity note), CLAUDE.md (229 tests, the supply-fidelity test-strategy bullet, the updated boundary/wire/action bullets, the nullable-condition data-layer note), PAD v1.10 (revision block [R11], §4.1, §7.1, §10), and this session record.

**Session 17 complete — the supply surface now matches the live's measured contracts exactly: the empty quantity (stores "", bare "qty" chip label, blank detail, `qty:""` export, verbatim import round-trip), the format gate with the live's exact error copy, and the two-state condition (inert create select, absent "?" vs explicit "OK"/"✓ ok", `status:"ok"` export). Every other surface re-confirmed at parity on fresh evidence, plus the atomic deleteProject and the corrected README.**

**Verification:** all claims executed and observed this session; the live account is pristine; the accepted divergences remain documented (focus ring, dev badge, account data, the live's residue message, the mobile login card's 0.5px fractional offset, the nav-vs-aside landmark, dev-mode-only browser artifacts).

**Suggested next steps:** watch the CI verify-gate run on the pushed commit; the evidence directory (`download/parity-evidence/r11-*`) holds the reference screenshots for future regression comparisons; if the live ever changes its supply modal's inert select or the quantity/condition contracts, re-run the create/edit/export/import probes before touching the clone.
