Session 26 — the verification-hardening pass, continuing from session 24's focus & keyboard-contract parity and the user's session_25 transcript (main @ d6da2fa, 271 tests green, r11–r15 contracts pinned; the user's prompt for this round asked for a full remediation audit with the mobile navigation menu as a named concern, the db/ folder at the repo root, a Vitest + Playwright test suite, screenshots, docs alignment, and a skill-file distillation).

Started from a fresh clone after another workspace reset: core docs re-read and validated against the codebase (README / AGENTS / CLAUDE / PAD v1.14 / session_24 / session_25), baseline gates green (lint ✓, typecheck ✓, 271/271 ✓), and both browser sessions (live + clone) re-established — the live account pristine (PROJECTS 0 / SUPPLIES 0 / INSPO 15), signed in with the provided credentials.

Reconnaissance (live vs clone, 2026-09-22, fresh this session):

- 8 paired captures with VLM comparisons and pixel structural diffs at the documented r13/r14/r15 baselines (dashboard 0.67%, projects 0.66%, supplies 0.66%, inspiration 0.80%, mobile-dashboard 0.29%, sidebar drawer 0.49%, chat drawer 0.92%, login 0.18%) — early high readings (5–17%) decomposed into capture-timing artifacts (the live's async loading states, a view mismatch, the pointer parked on a live control), and the disciplined re-captures landed every pair back at its documented value: **visual parity intact, zero real divergences**.
- **The mobile navigation menu — the user-critical surface — re-verified end-to-end against the live at 390×844**: the "☰ Studio Tools" toggle opens the left drawer at the live's geometry (312px wide, x=0, z-50, slides from x=-312 over 0.3s), ONE shared scrim (`fixed inset-0 z-40 bg-black/60 md:hidden` — plain dim, no backdrop blur, below the drawers, instant unmount), drawer navigation switches the view AND closes the drawer, the scrim click dismisses without navigating, the closed chat drawer carries `inert` and sits off-screen at x=390, Escape is a no-op on both drawers (the live ignores it), and the "Chat ☰" toggle opens the right drawer at x=78. Every measured value matches the live exactly.
- Functional: 23/23 smoke checks on the dev server; the export payload byte-identical.

The audit's real findings were all TOOLING-side (the UI was already at parity):

**r16-F1 (HIGH) — the SQLite path was runtime-dependent, so the documented `file:../db/custom.db` contract was nondeterministic.** `.env.example` documented a `src/lib/db-path.ts` that did not exist, and Prisma's own resolution splits by runtime: the CLI and Node-side tooling resolve a relative `file:` URL against the CWD (the DB silently landing at `/home/z/my-project/db/custom.db`, OUTSIDE the repo), while the bun runtime resolves it schema-relative (inside the repo). Two runtimes, two database files — and a workspace shell exporting an absolute `DATABASE_URL` hijacked both via standard env precedence. Remediation (TDD, 12 new tests in `src/lib/db-path.test.ts`): the new `src/lib/db-path.ts` pins ONE contract — relative `file:` URLs resolve against `prisma/schema.prisma` (a module-anchored `schemaDir()` with an `AST_PRISMA_DIR` escape hatch and a bounded CWD walk), absolute and non-file URLs pass through unchanged (the action layer's throwaway temp DBs depend on that), and repo tooling (`resolveRepoDatabaseUrl`) prefers the repo's OWN `.env` over an inherited absolute URL. Wired as `PrismaClient`'s `datasourceUrl` in `src/lib/db.ts` and the seed, and as a `DATABASE_URL="$(bun scripts/prisma-url.ts)"` prefix on the `db:*`/`dev`/`start` scripts so the CLI gets the same authoritative absolute path. Verified empirically: the CLI, the dev server, and the runtime now all land on `<repo>/db/custom.db` (the dev-server login + health probe confirmed), 283/283 tests green, and the stray outer DB deleted.

**r16-F2 (HIGH) — the required Playwright E2E suite.** `playwright.config.ts` + 8 spec files (25 specs): a `setup` project signs in ONCE through the real UI and saves a `storageState` (per-test sign-ins would self-throttle the pinned 5-attempts/minute rate limiter), a `chromium` project at the 1536×844 parity viewport covers auth / dashboard / import-export / projects / supplies, and a `mobile-chromium` project at 390×844 pins the mobile navigation contract (drawer geometry measured after the 0.3s slide settles, the shared scrim's classes + z-index + the Tailwind-v4 `oklab(0 0 0 / 0.6)` computed serialization of `bg-black/60`, inert, the Escape no-op, the Chat toggle at x=78). Serial single-worker over the one shared SQLite; every created row is deleted through the real UI (native confirms auto-accepted). The spec authoring itself took six debug iterations — the failure modes are worth recording: accessible names carry the DOM's mixed-case text (the tiles' ALL-CAPS look is a CSS `uppercase` transform, invisible to roles); role name matching is case-insensitive substring ("STUDIO CHAT" hits both the panel's and the drawer's h2s; "Category" ⊂ "Subcategory"); the chat body and the sidebar rail render in BOTH the desktop panel and the `md:hidden` mobile drawer copies (`getByText` matches both, `getByRole` filters by visibility — scope or use `:visible`); the projects view's top level is the live's TILE grid, the chips live in sub-views; one supplies spec initially LEAKED its supply (a count-0 assertion that only proved the grid hides names, not that the row was deleted) and its residue cascaded into three later failures until the UI-cleanup walk (Paint tile → All Paint → chip → Delete) was added; the sub-views carry a bare "Projects" breadcrumb button that a loose `/^Projects/` also hits (`/^Projects \d/` pins the stat tile). Final state: **25/25 green**, and the suite doubles as executable documentation of the golden paths.

**r16-F3 (MEDIUM) — the CI verify-gate workflow was corrupted.** Both `branches:` lists read `ain]` (invalid YAML — the gate would never have triggered). Repaired to `[main]`, and the workflow gained a second `e2e` job (Playwright's chromium → `db:push` + `db:seed` at the repo-root `db/` contract → `bun run test:e2e`, which boots its own dev server) alongside the existing lint/typecheck/test/build `verify` job against its throwaway `/tmp` DB.

Smaller hardening shipped with them: `db:seed` runs under plain `bun` (no network-fetched `tsx`; the seed has no path aliases); `.env.example` corrected to the real test path (`src/lib/db-path.test.ts`); `.gitignore` covers `e2e/.auth/`, `test-results/`, and `playwright-report/`; the Turbopack corrupted-cache trap (session_25's lesson) was hit and cleared once during the E2E bring-up.

Verification: lint ✓, typecheck ✓, **283/283 vitest** ✓, **25/25 Playwright E2E** ✓, valid workflow YAML ✓, and the 8 `docs/screenshots/` re-shot on the remediated dev server with the steady-state + neutral-pointer discipline — 7/8 pixel-identical to this session's parity-verified clone references, the sidebar drawer at 1.15% (a single truncated text fragment at the drawer edge; sub-perceptual class).

Documentation aligned: README (the `test:e2e` command + Playwright paragraph, 283-test counts, the r16 status row), AGENTS.md (the test:e2e command row, the 283-test row with the SQLite path contract first, the Environment section now explaining the resolver + script prefixes), CLAUDE.md (the commands table, the db-path test bullet, the two-suite browser paragraph), PAD v1.15 (revision block [R16], §7.1 distribution 255 + 28 + the E2E row, §7.4 checklist, §8.4 the two-job pipeline), `.env.example`, and this session record. `art-supply-tracker_SKILL.md` distilled per the repo's own distill-codebase-skill method.

**Session 26 complete — the clone's UI was already at parity (re-confirmed on fresh paired evidence, the mobile navigation menu included, measured value-for-value against the live); this round hardened the ground under it: one deterministic SQLite path contract, a 25-spec Playwright E2E suite pinning the golden paths and the mobile drawer contract, a repaired two-job CI gate, and refreshed screenshots + docs.**

**Verification:** all claims executed and observed this session; the live account left pristine (read-only recon); the accepted divergences remain documented (dev badge, account data, the live's residue message with its 78px chat-column offset, the mobile login card's 0.5px fractional offset, the nav-vs-aside drawer landmark, dev-mode-only browser artifacts, sub-perceptual oklab-vs-rgba compositing and fractional text-row rounding).

**Suggested next steps:** watch the CI verify-gate's first `e2e` job run on the pushed commit; the E2E suite is the new regression backbone — extend it before touching the browser layer (a failing spec leaves its row behind, so a residue cleanup pass or a re-seed precedes a debugging run); candidate probe dimensions still unexplored: screen-reader announcement order (live-region behavior), reduced-motion rendering, and print stylesheet parity; re-shoot `docs/screenshots/` after any chrome-level change (pointer parked neutrally).
