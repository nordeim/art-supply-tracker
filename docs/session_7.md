Continuing from session 6's verified state (main @ e55b929, all gates green, visual parity reported), this session re-audited the clone against the live site with fresh eyes and found the parity claim was premature in three areas — then closed every gap with TDD.

Reconnaissance first: logged into the live site with the operator's account, captured all four views plus the login screen, both create modals, the project detail panel, the supply detail panel, the supply chip states, the mobile drawer, and the live export JSON (via a URL.createObjectURL hook). Downloaded the live JS bundle and extracted its exact edit-form JSX, the Jz condition map, the category-tile markup, and the two unassigned-option strings.

**Finding 1 — the token regression.** The live app carries TWO color systems: a `:root` CSS-variable block and the compiled Tailwind utilities, and they disagree for three tokens. Session r3 had "corrected" the clone's purple/yellow/coral to the `:root` values (#5b3fd3/#f4f27a/#ffe0cc) — but the `:root` variables are vestigial; nothing in the live bundle resolves them. The rendered truth lives in the utilities: `.text-ast_purple` = rgb(90,58,142), `.text-ast_yellow` = rgb(255,213,168), `.text-ast_coral` = rgb(255,122,122). Verified three ways: computed styles on the live DOM (the modal Cancel button computes #ffd5a8), the live stylesheet's `.text-ast_yellow{color:rgb(255 213 168…)}` rules, and the scrollbar thumb's `linear-gradient(rgb(255,77,184), rgb(90,58,142))`. Red phase first: a new `design-tokens.test.ts` reads globals.css and pins every `@theme` literal to the utility ground truth (purple #5a3a8e, yellow #ffd5a8, coral #ff7a7a + the six glow shadows). Green: three token edits. The main-card border now computes rgba(90,58,142,0.5) — byte-equal to live.

**Finding 2 — edit UX.** The live app does not edit in modals. Both Edit buttons swap the detail panel for an INLINE form in the same chip-row slot: "Edit Project" is a single-column turquoise panel (Title, Status, Notes, Budget defaulting to 0, Photos) whose status select orders Planned first — the opposite of the create modal; "Edit Supply" is the pink panel with the Name+Photo row (preview, × remove, "Change photo"), a 2-col grid (Category, Quantity "e.g., 2 or 1/2", Stock Status, Location, Notes as a single-line INPUT), the conditional Subcategory/Custom-Category block with the "e.g., Dry brush, Palette knife…" free-form input, Barcode "e.g., 012345678901", and "Add to Project" with the current-assignment chip. The clone had edit modals. Rebuilt both as `project-edit-panel.tsx` / `supply-edit-panel.tsx`, reworked the two create modals to create-only, extracted the duplicated photo-downscale helper into `photo-data-url.ts`, and wired the views' panel slots to swap detail ↔ edit in place. Budget round-trip helpers (`budgetEditValue`/`budgetFromEditInput`) and the two unassigned-option strings are pinned in the domain tests (edit: "— Studio inventory (unassigned) —", create: "Unassigned — Studio inventory").

**Finding 3 — login chrome.** The live login is AWS Amplify's stock dark UI, not the studio's rounded aesthetic: a sharp-cornered #120724 card with a 1px #5B3FD3 border (literal, from custom CSS — NOT the utility purple), text-only tabs (active #047d95, inactive #304050, weight 700), 4px-radius inputs with #89949b borders on transparent backgrounds at 42px height, labels #304050 at 16px, a #FE5FA7 primary at radius 4, a #047d95 forgot-link, and an eye-ICON show-password control announced as `role="switch"` with aria-checked. Pixel-sampled every color from the live screenshot to pin them. The clone had rounded-xl everything with a "Show" text toggle — restyled to match; VLM now reports NEAR-IDENTICAL.

Smaller gaps closed in the same pass: the create modal gained the live "Other / Custom…" free-form subcategory input (and the Zod schema relaxed to live parity — its save path stores free-form text; the pickers are the vocabulary guard, so the cross-list rejection test was inverted to an acceptance test); the supply modal's grids became `grid-cols-1 md:grid-cols-2` (stacks on mobile like live); the supply chip dropped `w-full`+`truncate` (live names wrap); the chat input's extra aria-label removed (live names it by placeholder alone); the ✧ span re-exposed (live's accessible name includes it); exports now emit unset `budget`/`barcode` as `""` (the live in-memory default, confirmed by the captured live export) plus `imageUrl: null` in the live field order.

Verification: lint ✓, typecheck ✓, 109/109 tests ✓ (was 97), production build ✓. Browser-driven round-trips on the running app: login (demo account), create project/supply, chip → detail panel, panel → inline edit → save → detail panel returns updated (renamed a project, updated a supply location), Low Stock filter, delete with native confirm, export blob inspected. VLM comparisons of all four views + login + edit panels vs the live screenshots: NEAR-IDENTICAL (remaining deltas are account data and the dev-tools badge, absent in production mode — re-verified in a production-mode run). Test data cleaned from both the clone and the live account (live left pristine: PROJECTS 0, SUPPLIES 0).

Documentation aligned: README (features, 109-test description, token table with the vestigial-`:root` caveat, r4 status row), AGENTS.md (test count, token-pinning rule, inline-edit invariant, subcategory boundary wording), CLAUDE.md (suite description, golden path 4, login literal-border exception), PAD v1.3 (revision block, token table, test distribution 91+18, design-tokens test pattern), and this record.

**Session 7 complete — token, edit-flow, and login parity achieved.**

The clone now renders the live app's exact palette (utility ground truth), edits in place with the live panels, and greets with the live Amplify chrome — all guarded by the new design-tokens test so the `:root`-vs-utility trap cannot recur.

**Verification:** all claims executed and observed this session; the live account was left pristine; the only accepted divergences remain documented (supply status persistence, single-membership assignment per ADR-008).

**Suggested next steps:** watch the CI verify-gate on the pushed commit; consider promoting the manual golden-path checklist into the Playwright E2E runner flagged in PAD §10.
