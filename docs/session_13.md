Session 13 — the error-state pass, continuing from session 11/12's verified pixel parity (main @ 580a71d, 180 tests green, r8 Amplify chrome pinned).

Started from the pushed tree after a workspace refresh: remote main had moved one commit (`580a71d` — docs/session_12.md, the parallel r8 agent's terminal log; no code changes), pulled fast-forward. Environment rebuilt (`db:push`, `db:seed`), all gates re-verified green (lint, typecheck, 180 tests, clean production build; skills/ excluded from every gate), the 23-check smoke suite passed on the fresh tree, and the core docs (AGENTS, CLAUDE, README, PAD v1.7, session 11/12) were re-read against the source. The live account was pristine (PROJECTS 0 / SUPPLIES 0 / INSPO 15).

Reconnaissance (live vs clone, both DOMs measured side by side):

- Four desktop views + six mobile captures (dashboard, supplies, projects, inspiration, the sidebar drawer, the chat drawer) + the login page at both viewports captured on both apps; 17 VLM comparisons: ALL PARITY. (One capture artifact first round: the live's closed mobile drawer is NOT inert — find-by-role clicks on hidden drawer buttons "succeed" while visually missing, so the live mobile supplies/projects captures initially shot the stale inspiration view; re-captured through proper drawer navigation.)
- DOM spot-checks all byte-identical: desktop header 1920×88 with logo (24,16) 222×56; mobile header 390×126 with logo (24,43) 159×40; chat structure (Community 1620/121, STUDIO CHAT 1637/362, messages 1669/414 and 1669/632); login card (494,464,480×429) with the #5B3FD3 border; tab strip (495,465,478×50) with 239×50 tabs; password row input 364×42 (4px-left radius) + eye 50×42 (4px-right radius); Create Account labels and switches identical.
- With the steady-state surfaces confirmed at parity, the session pivoted to the ERROR STATES — the one surface no previous session had deep-dived (r8 had verified only the sign-in error's text/size/color, not its container chrome).

Findings and remediation (all TDD — RED first, then GREEN, then browser-verified against the live):

1. **r9-F1 (P2) the Amplify error alert box:** the live renders SERVER auth errors (bad credentials, duplicate email, invalid reset code) inside a dismissible alert — `div[role=alert]`, flex row, items-center, 16px gap, px-4 py-3, bg rgb(252,233,233) = #FCE9E9, NO radius/border, content-driven height (58px one line — the 34px dismiss button + 24px v-padding sets the floor; 72px when the message wraps). Anatomy (outerHTML extracted from the live): a 24×24 warning icon (Material "error" path, #660000, in a 1em span), the 16px/400 #660000 message in a `flex: 1 1 0%` wrapper, and a 50×34 "Dismiss alert" button (transparent bg, 1px transparent border, 4px radius, fw 700, the 16×16 X path) whose click removes the alert and lets the vertically-centered card re-settle to the no-error layout (submit 776 → 739). The clone rendered a bare `<p>`. Fixed: an in-file `AmplifyAlert` component with the exact chrome and SVG paths. Verified byte-identical: desktop alert (527,702) 414×58 with icon (543,719) 24×24, text 276×24, dismiss (875,714) 50×34; mobile alert (49,778) 292×72 (the message wraps at 186px text width); dismiss restores the layout on both.
2. **r9-F2 (P1) the Reset Password confirmation flow:** on the live, "Send code" with a VALID email transitions the card to a confirmation view — h3 "Reset Password" (527,469, 414×40, 32px/500 #0d1a26), "Code *" label (the literal asterisk) with a full-width 414×42 text input, "New Password" + "Confirm Password" (364×42 inputs + 50×42 eyes), a 414×42 #FE5FA7 Submit, and a centered content-width "Resend Code" link (676,853, 115×35) — card (494,436,480×485). Submitting a wrong code renders the alert box "Invalid verification code provided, please try again." (414×72 at y=751); a password mismatch renders the inline "Your passwords must match" (y=775); "Resend Code" is a silent no-op; there is NO back button. The clone instead dead-ended on a static "Password reset is not configured…" notice (an r8 invention the live never shows). Fixed: the full confirmation view with the live's geometry, the invalid-code rejection (no mailer exists — ADR-003 — so no code can ever be valid; the honest simulation of the deployed behavior), the mismatch/policy validation, and the silent resend. Verified: every element at the live's exact coordinates, both no-error and error states.
3. **r9-F3 (P3) duplicate-email copy:** the live's Cognito answers "User already exists" (measured in the alert box at y=780); the clone said "An account with this email already exists. Try signing in." Fixed in `auth.ts` (both the findUnique path and the unique-constraint race path).
4. **r9-F4 (P2) the Cognito password-policy stack:** on the live, submitting a weak signup password renders EVERY violated rule as its own contiguous 24px inline line directly after the Password field — "Password must have at least 8 characters" / "…upper case letters" / "…lower case letters" / "…numbers" / "…special characters" (16px/400 #660000, no vertical gaps) — coexisting with the mismatch line after Confirm; submission is blocked. The live's password inputs carry NO min/maxLength attributes (policy is client-JS + server). Fixed: `passwordPolicyViolations()` in `validation.ts` (the single source, in the live's copy and display order), the client stack in the signup and reset-confirmation forms, and a server-side mirror in `signUpSchema` (defense in depth). Verified DOM-identical in the abc/abc state (lines 671/695/719/723, confirm label 783, submit 873), the abc/xyz state (651/675/699/723 + mismatch 853), and the confirmation view's policy state (649/673/697/721, confirm 761) — including the centered-card re-settling shifts, which match the live's arithmetic exactly.
5. **r9-F5 (P3) native form validation:** the live's auth forms do NOT suppress browser validation, so empty and malformed-email submissions are natively blocked (no DOM error — verified by an empty sign-in submit); and signing in with a short password SUBMITS and fails with "Incorrect username or password." (no client policy check). The clone's forms used `noValidate` + input min/maxLength, surfacing server errors for natively-blockable cases and pre-rejecting short sign-in passwords. Fixed: no suppressed validation, password inputs `required`-only, and `signInSchema` made permissive (any 1–256-char password attempts and fails; `signUpSchema` keeps the policy mirror server-side).
6. **r9-F6 (P3) content-width link buttons:** the live's Forgot (643,797, 182×35), Back to Sign In (670,763, 127×35), and Resend Code links are content-width 35px centered (`amplify-button--small`: 6px/12px padding + 1px transparent border); the clone rendered Forgot as a full-width 51px block (the r8 pin was a pre-hydration artifact). Fixed with a centering wrapper + the live's padding; verified (643,797,182×35) and (670,763,127×35) byte-identical, with the no-error card (480×429) and submit (739) regressions intact.

Verification: lint ✓, typecheck ✓, 202/202 tests ✓ (180 + 22 new: 14 login-fidelity pins incl. the alert chrome/policy stack/confirmation view/link buttons/native-validation attribute set, 8 validation pins for the policy rules, the permissive sign-in schema, and the server mirror), 23/23 smoke checks ✓, production build from a clean `.next` ✓, and VLM parity on the new error-state pairs (desktop error, mobile error, reset-confirmation error — PARITY; the mobile comparison's only flagged delta was the expected Next.js dev badge). The live account was re-verified pristine after the session's recon (sign-in/sign-out only; the failed sign-ins and reset attempts created no data — Cognito, like the clone's rate limiter, throttles but does not lock).

Ops notes: the live browser session stalled once mid-recon (CDP timeouts) — closed and reopened a fresh session, no state lost; the earlier live measurements that looked 20–37px shifted were traced to stale alerts (the live does not auto-dismiss previous errors) — always re-baseline the DOM state before cross-checking geometry. The r8 lesson generalizes: an element's chrome can only be trusted from a freshly-triggered, hydrated state.

Documentation aligned: README (r9 status row, auth-gate feature rewrite, 202-test description, verification row), AGENTS.md (202 tests, the two-tier error chrome + reset-flow invariants), CLAUDE.md (test-strategy bullets for the policy rules, the permissive sign-in schema, and the extended login-fidelity pins), PAD v1.8 (revision block [R9], §7.1 distribution 179 unit + 23 action, §10 the r9 resolution row), and this session record.

**Session 13 complete — the auth error chrome now matches the live at pixel level: the Amplify alert box (with its exact icons and working dismiss), the Cognito policy-rule stack, the duplicate-email copy, the full Reset Password confirmation flow, native validation, and the content-width link buttons are all measured-identical to the deployed app.**

**Verification:** all claims executed and observed this session; the live account is pristine; the only accepted divergences remain documented (focus ring, dev badge, account data).

**Suggested next steps:** watch the CI verify-gate run on the pushed commit; the evidence directory (`download/parity-evidence/r9-*`) holds the reference screenshots (including the error states) for future regression comparisons; if the live's Cognito policy or Amplify theme ever changes, re-run the error-state probes before touching the clone.
