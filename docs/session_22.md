Session 22 — the default-palette pin pass, continuing from session 21's verified hover/hyphen-family parity (main @ 26582ba, 247 tests green, r13 contracts pinned; 26582ba added the user's r13 transcript as docs/session_21.md).

Started from the pushed r13 tree: core docs re-read and validated against the codebase (README / AGENTS / CLAUDE / PAD v1.12 / session_20 / session_21), baseline gates green (lint ✓, typecheck ✓, 247/247 ✓), `.env.example` re-verified against the codebase's sole environment variable (`DATABASE_URL`), and the 8 `docs/screenshots/` re-verified present. Both browser sessions (live + clone) were re-established at 1536×844 and 390×844 — the live account pristine (PROJECTS 0 / SUPPLIES 0 / INSPO 15).

Reconnaissance (live vs clone, 2026-09-19, fresh this session):

- 8 paired captures (desktop dashboard / projects / supplies / inspiration, mobile dashboard / sidebar drawer / chat drawer, login — the pointer parked at a verified non-hoverable spot for every capture, per the r13 hygiene rule) with VLM comparisons: 6 direct PARITY plus 2 flags that map 1:1 onto the documented accepted set — the dashboard-mobile pair's flags were exactly the header email + dev badge (the VLM self-corrected to PARITY in its own answer), and the chat-drawer pair's flags were exactly the live's residue message ("Hello from clone test") and its input row pushed below the 844px fold by the documented 78px scrollHeight delta (928 vs 850, re-measured this session).
- Pixel structural diffs: 0.23–1.14% hot per pair — the SAME values r13 measured per pair (dashboard-desktop 0.72%, chat-drawer 1.14%, login 0.23%), every cluster classified into the documented accepted set (residue message + input-row offset, dev badge, header email glyphs, Partners-card fractional rounding, login antialiasing).
- Functional probes: the pristine-state export payload byte-identical (121 bytes both sides, modulo the timestamp); 23/23 smoke checks on the dev server; the sign-out flow exercised on both sides during the login capture; the r12 lavender active-tile fix re-verified intact (computed colors + geometry 106×78 @ (257,173) byte-identical to the r12 record) and the r13 memory-button fix re-verified intact (idle border #5b3fd3/30, hover trio engaging with text rgb(244,242,122) byte-exact).
- NEW probe dimension: a systematic DOM-level hover-contract sweep across both sessions — the probe class that found r13-F1 (a hover state that never rendered in captures), now extended to every interactive element (stat tiles, create button, rail buttons, import/export, sign-out, memory button). The create-project button verified PARITY (gradient endpoints byte-identical; the `in oklab` interpolation form is the documented sub-perceptual class).

**r14-F1 (Medium) — Tailwind v4's DEFAULT palette drifts from the live's Tailwind v3 values on every default-family class the app uses.** Seven surfaces use DEFAULT-family classes rather than the pinned ast-* tokens: the Sign Out button (`border-pink-400/60` + `text-pink-200` + `hover:bg-pink-500/20`), the memory button's text (`text-pink-300`), the header email gradient (`from-cyan-400 via-blue-500 to-pink-500`), the barcode fields' focus trio (`border-blue-500/40` / `focus:border-blue-400` / `focus:ring-blue-500/30`, in both the create modal and the edit panel), and the chat error alert (`text-pink-300`). Tailwind v4 re-derived its default palette in oklch, so those classes resolved drifted values: pink-400 #fb64b6 vs the live's #f472b6 (14/channel on the always-visible Sign Out border), cyan-400 #00d2ef vs #22d3ee (34 in the red channel of the email gradient), blue-500 #3080ff vs #3b82f6, pink-500 #f6339a vs #ec4899, pink-300 #fda5d5 vs #f9a8d4, pink-200 #fccee8 vs #fbcfe8, blue-400 #54a2ff vs #60a5fa. All live values were extracted from the live's compiled CSS (index-BNZKRdTa.css: `.border-pink-400\/60{#f472b699}`, `.text-pink-300{rgb(249 168 212)}`, `.from-cyan-400{#22d3ee}`, `.focus\:border-blue-400:focus{rgb(96 165 250)}`, etc.) and every drift pair was canvas-readback-verified as a real rendered delta. Like r13-F1's hovers, the drift is sub-threshold in screenshot diffs (the Sign Out border's Δ22 summed vs the 30 hot-pixel threshold) — invisible to the capture methodology, which is why ten prior rounds of pixel diffs never surfaced it; the hover-contract sweep (comparing computed styles at the DOM level) is what found it.

Remediation (TDD — RED first, then GREEN, then verified against the live at three levels):

- Mechanism spike BEFORE planning around the fix (the r13 discipline): a user `@theme` override REPLACES Tailwind v4's default theme emission entirely — the pinned variable emits as a single declaration with NO lab() re-declaration surviving, and the baked fallback rules become byte-identical to the live's compiled rules. The initial no-op reading was the wedged-Turbopack stale-CSS trap (the r11/r13 known issue) — a clean `.next` restart confirmed the mechanism. The spike was reverted before the real change.
- `src/app/globals.css` `@theme`: seven new pins — `--color-pink-200: #fbcfe8`, `--color-pink-300: #f9a8d4`, `--color-pink-400: #f472b6`, `--color-pink-500: #ec4899`, `--color-cyan-400: #22d3ee`, `--color-blue-400: #60a5fa`, `--color-blue-500: #3b82f6` — with a docblock recording the drift, the surfaces, and the do-not-use-other-default-families rule.
- The usage sites KEEP the live's class strings verbatim (class-string parity with the live DOM — the original cloning intent; the pins make the default families safe, so no literals are needed, unlike the r13 hyphen-family case where the live itself carried two parallel systems).
- `src/lib/design-tokens.test.ts` +13 pins: the seven positive token pins, a negative pin rejecting the TW4 drift values (#fb64b6, #f6339a, #00d2ef, #3080ff, #fda5d5, #fccee8, #54a2ff), and five usage-site class-string pins (the Sign Out button, the memory button's text, the email gradient, the chat alert, and the barcode fields in both files — guarding against a future "fix" that swaps a class to a literal or to the ast-* families). RED: 8 failed / 5 passed; GREEN: 260/260.

Verification: lint ✓, typecheck ✓, **260/260 tests** ✓ (247 + 13), clean production build ✓. The emitted dev AND production CSS carry the pinned values — `.border-pink-400\/60{border-color:#f472b699}` and `.border-blue-500\/40{border-color:#3b82f666}` are byte-identical to the live's rules (the `@supports` color-mix forms resolve the pinned variables, pure alpha fades — the r13-verified equivalence class). Browser-verified end-to-end: the Sign Out idle border computes oklab(#f472b6)/0.6 with canvas readback maxd=0 against the live's rgba(244,114,182,0.6); the Sign Out text and hover bg, the memory button's text, and all three email gradient stops are byte-identical rgb forms; the full 7-token canvas readback is maxd=0 on every pair. A post-fix paired dashboard capture re-diffed at the unchanged 0.72% (expected — the drift was sub-threshold; the header cluster decomposes into the email-glyph divergence, whose 1.59px group shift was re-measured and re-attributed to the documented email-width delta, with the Sign Out region at 0 hot pixels). The 23/23 smoke suite re-run green post-fix. Evidence in `download/parity-evidence/r14/`.

Deliverables beyond the fix: the 8 `docs/screenshots/` re-shot on the remediated dev server (login, desktop dashboard/projects/supplies/inspiration, mobile dashboard/sidebar drawer/chat drawer — pointer parked neutral for every capture); `.env.example` verified to match the codebase (`DATABASE_URL` only).

Documentation aligned: README (r14 status row, 260-test counts, the design table's two new default-palette rows), AGENTS.md (260 tests + the default-palette-pin framework quirk), CLAUDE.md (260 tests + the Tailwind bullet + the default-palette test-suite bullet), PAD v1.13 (revision block [R14], §5.2 default-palette pins, §7.1 unit distribution 232 + 28, §10 resolved row), and this session record.

**Session 22 complete — the app's Tailwind DEFAULT palette now renders the live's Tailwind-v3 values exactly, verified byte-identical at the CSS-rule, computed-style, and rendered-pixel levels, with the class-string parity contract pinned. Every steady-state surface re-confirmed at parity on fresh paired evidence; the r11 supply contracts, the r12 stat-tile accents, the r13 hover/hyphen-family fixes, and the r10 drawer chrome remain pinned.**

**Verification:** all claims executed and observed this session; the live account is pristine (read-only recon — no mutations beyond the sign-in/sign-out flow); the accepted divergences remain documented (focus ring shorthand, dev badge, account data, the live's residue message with its 78px chat-column offset, the mobile login card's 0.5px fractional offset, the nav-vs-aside drawer landmark, dev-mode-only browser artifacts, sub-perceptual oklab-vs-rgba compositing and fractional text-row rounding).

**Suggested next steps:** watch the CI verify-gate run on the pushed commit; the hover-contract sweep script (`scripts/r14_hover_sweep.py` pattern, evidence under `download/parity-evidence/r14/`) is the template for future recons — computed-style-level comparison catches what captures cannot; if a future round adds any NEW Tailwind default-palette class, pin its family to the live's v3 value first (the AGENTS.md quirk now says so); re-shoot `docs/screenshots/` after any chrome-level change (pointer parked neutrally).
