# SQLite datasource — Prisma resolves this at runtime.
# Relative paths resolve from prisma/schema.prisma, so ../db/custom.db
# points at ./db/custom.db in the repo root (gitignored, created by `bun run db:push`).
DATABASE_URL=file:../db/custom.db
